apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "petmate-service.fullname" . }}-test-connection"
  labels:
    {{- include "petmate-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "petmate-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
