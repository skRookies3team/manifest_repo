apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthcare-service.fullname" . }}
  labels:
    {{- include "healthcare-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthcare-service.selectorLabels" . | nindent 4 }}
