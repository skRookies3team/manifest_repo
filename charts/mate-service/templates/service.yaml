apiVersion: v1
kind: Service
metadata:
  name: {{ include "petmate-service.fullname" . }}
  labels:
    {{- include "petmate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "petmate-service.selectorLabels" . | nindent 4 }}
